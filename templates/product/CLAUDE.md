# Claude Code adapter

Read and follow `AGENTS.md` in this repository. It is the canonical product working agreement shared with Codex and OpenCode.

Claude Code-specific configuration may live in `.claude/`, but it must not override or duplicate the portable product context without a documented reason.
